services:
  mysql-db:
    image: mysql:8.0
    container_name: mysql-server
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=12345678
      - MYSQL_DATABASE=university_db
    # ป้องกันปัญหาแรมไม่พอบน Mac
    deploy:
      resources:
        limits:
          memory: 1G